# Apps

Deployable applications live here.

- `web/`: Frontend web application.
- `api/`: Backend API and worker application.

Application code should stay inside the appropriate app and depend on shared capabilities through `packages/`.
