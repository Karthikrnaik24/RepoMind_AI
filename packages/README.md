# Packages

Reusable internal packages live here.

Packages should expose focused capabilities that can be shared by `apps/web`, `apps/api`, workers, scripts, or future services without creating circular dependencies.

Package boundaries should remain small and explicit.
