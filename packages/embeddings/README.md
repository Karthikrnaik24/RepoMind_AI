# Embeddings Package

This package will contain embedding-related abstractions.

Intended responsibilities:

- Embedding provider interfaces.
- Embedding request and response contracts.
- Embedding model metadata handling.
- Helpers for detecting stale embeddings.

Embedding generation should remain decoupled from a specific provider.
